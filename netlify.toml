[build]
  command = "node build-function.mjs && npm install --prefix server && npm run build"
  publish = "dist"

[functions]
  directory = "netlify-dist-functions"
  node_bundler = "nft"

[functions."api"]
  timeout = 26

# Proxy /api/* to the Netlify Function
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/api/:splat"
  status = 200

# SPA fallback — must be last
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
